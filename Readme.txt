In the servlet, for the dao findall() method, in doget() call this dao method. 
in dopost() method, to find an entity by id/code. In the html file create form to accept id.
Call the dao method for this and display in the data in jsp file created.

Validate the id taken from the form by creating another dao method.Refer
the CustomerDAO.

